spring.application.name=credit-recovery-service

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/credit_recovery
spring.datasource.username=credit_recovery
spring.datasource.password=credit_recovery

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false